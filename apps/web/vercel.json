{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm turbo build --filter=@expat-atlas/web",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/source-monitor",
      "schedule": "17 9 * * *"
    }
  ]
}
