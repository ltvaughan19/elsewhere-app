import { MarketingStub } from "@/components/marketing-stub";

export default function BecomePartnerPage() {
  return (
    <MarketingStub
      title="Become a partner"
      description="Apply to join the Expat Atlas partner network. All applications are manually reviewed."
    />
  );
}
