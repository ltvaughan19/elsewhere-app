import { MarketingStub } from "@/components/marketing-stub";

export default function BlogPage() {
  return (
    <MarketingStub
      title="Expat Atlas journal"
      description="Editorial guides on visas, budgets, and relocation planning — always linked to our source ledger, never guru advice."
    />
  );
}
